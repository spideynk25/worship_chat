import 'package:flutter/material.dart';

// ─── Core Dark Theme ───────────────────────────────────────────────────────
const backgroundColor     = Color(0xFF0A0A0F); // Deep near-black with cool tint
const appBarColor         = Color(0xFF12121C); // Slightly lighter for contrast
const webAppBarColor      = Color(0xFF1A1A26);

// ─── Accent Palette (Pink > Orange) ────────────────────────────────────────
const tabColor            = Color(0xFFFF2D78); // Hot pink — primary accent
const accentOrange        = Color(0xFFFF6B2D); // Vibrant orange — secondary

// ─── Message Bubbles ────────────────────────────────────────────────────────
const messageColor        = Color(0xFF5C1A3A); // Sent (my) bubble — deep pink
const senderMessageColor  = Color(0xFF1E1A28); // Received bubble — dark purple

// ─── UI Surface Colors ──────────────────────────────────────────────────────
const mobileChatBoxColor  = Color(0xFF1C1C28); // Chat input background
const chatBarMessage      = Color(0xFF1C1C28); // Chat bar background
const searchBarColor      = Color(0xFF1E1E2E); // Search input background

// ─── Text & Misc ────────────────────────────────────────────────────────────
const textColor           = Color(0xFFF0EEF8); // Near-white, slightly warm
const dividerColor        = Color(0xFF2A2A3E); // Subtle divider line
const greyColor           = Color(0xFF8A8AA0); // Muted grey for secondary text
const blackColor          = Colors.black;
