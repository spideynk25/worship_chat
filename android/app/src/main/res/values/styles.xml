<?xml version="1.0" encoding="utf-8"?>
<!-- Create or update this file: android/app/src/main/res/values/styles.xml -->

<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    
    <!-- Normal theme for the app -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
    
   <style name="UCropTheme" parent="Theme.AppCompat.Light.NoActionBar">

        <!-- Make status bar transparent -->
        <item name="android:statusBarColor">@android:color/transparent</item>

        <!-- Force draw system bar backgrounds -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>

        <!-- Prevent system from applying white background -->
        <item name="android:forceDarkAllowed">false</item>

        <!-- VERY IMPORTANT: disable light status bar -->
        <item name="android:windowLightStatusBar">false</item>

        <!-- Don’t extend content under status bar -->
        <item name="android:fitsSystemWindows">true</item>

        <!-- Navigation bar (optional) -->
        <item name="android:navigationBarColor">@android:color/black</item>

    </style>


</resources>